/*
import API_BASE from "./config.js";

export default {
  getCode(data) {
    const {product,sessionId} = data
    const url = `${API_BASE}/supersign/getInstallCode?product=${product}&sessionId=${sessionId}`;
    return this.$axios.get(url);
  },
}*/